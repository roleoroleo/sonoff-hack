#!/bin/bash

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd $SCRIPT_DIR

mkdir -p ../../build/sonoff-hack/bin/

#rsync -a ./_install/bin/sqlite3 ../../build/sonoff-hack/bin/
cp -f ./_install/bin/sqlite3 ../../build/sonoff-hack/bin/
cp -f ./_install/lib/libsqlite3.so.0.8.6 ../../build/sonoff-hack/lib/libsqlite3.so.0
